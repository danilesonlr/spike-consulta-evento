package com.poc.repository;

import com.poc.domain.ContextoContrato;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ContextoContratoRepository extends JpaRepository<ContextoContrato, Long> {
}
